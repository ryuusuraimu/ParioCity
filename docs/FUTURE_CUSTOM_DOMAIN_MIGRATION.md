# Future Custom Domain Migration

## Current Plan

Do not buy `pariocity.com` yet.

Use the free Cloudflare Pages subdomain first:

```txt
https://pariocity.pages.dev
```

## When to Buy pariocity.com

Buy `pariocity.com` only after validation.

Suggested purchase triggers:

- 100 installs
- 20 active users
- 10 waitlist signups
- 3 willingness-to-pay signals

## Future URLs

Product page:

```txt
https://pariocity.com/pod-margin-lens
```

Privacy Policy:

```txt
https://pariocity.com/pod-margin-lens/privacy
```

Support:

```txt
https://pariocity.com/pod-margin-lens/support
```

## Migration Steps

1. Purchase `pariocity.com` only after validation.
2. Add `pariocity.com` as a custom domain in Cloudflare Pages.
3. Add `www.pariocity.com` if needed.
4. Confirm SSL is active.
5. Confirm all existing `pariocity.pages.dev` URLs still work.
6. Update Chrome Web Store URLs only after the custom domain is stable.

## Constraints

- Do not add analytics during migration.
- Do not add tracking during migration.
- Do not add payment during migration.
- Do not change POD Margin Lens Chrome Extension permissions.
